version: "3.9"

services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      APP_ENV: docker
    restart: unless-stopped
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000
